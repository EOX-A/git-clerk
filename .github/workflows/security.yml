name: Security Audit

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  security:
    name: Dependency Security Audit
    runs-on: ubuntu-latest

    steps:
      - name: Skip audit
        run: echo "Skipping security audit"
